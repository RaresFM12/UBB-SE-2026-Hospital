<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" Version="9.0.0" />
    <!-- OpenAPI package removed to prevent source-generator missing-type errors in this project. Re-add if OpenAPI support is required. -->
  </ItemGroup>

  <ItemGroup>
    <!-- Disabled EF Core design package that targets net10.0 to avoid restore errors in this workspace. If you need EF Core tooling for this project, re-enable and install matching runtime. -->
    <!-- <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.0-preview.4.25258.110">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference> -->
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Hospital.Data\Hospital.Data.csproj" />
    <ProjectReference Include="..\Hospital.Shared\Hospital.Shared.csproj" />
    <ProjectReference Include="..\Hospital.Services\Hospital.Services.csproj" />
  </ItemGroup>

<ItemGroup>
  <InternalsVisibleTo Include="Hospital.Tests" />
</ItemGroup>
</Project>
